# AI-Enriched Financial Audit Pipeline - Frontend

React frontend for the audit pipeline.

## Rules

- React class components only.
- No hooks.
- No Redux or unnecessary state framework.
- API calls are isolated behind service classes.

## Setup

```bash
npm install
copy .env.example .env
npm run dev
```

Backend must be running on the URL configured by VITE_API_BASE_URL.
